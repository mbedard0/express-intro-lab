<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Express Intro Lab</title>
  <%- include('../partials/head.ejs') %>
  <%- include('../partials/nav.ejs') %> 
</head>
<body>
  <h1>Student List</h1>
  <% if(error) {%>
    <h2><%= error %></h2>
  <% } %>
  <ul>
    <% student.forEach(function(students) {%>
      <li>
      <%= students.name%>,
      <%= students.age %>,
      <a href="/students/<%= students._id %>">
        Click for Details for Student <%= students._id%>
      </a>
      </li>
    <% }) %>   
  </ul>

</body>
<%- include('../partials/footer.ejs') %> 
</html>